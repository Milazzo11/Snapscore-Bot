Web-based Snapsore Bot

Selenium bot configued with undetectable chromedriver to increase Snapchat score.

[[ This program is functional, but the Snapchat web version does not seem to be configured to handle mass
   snap send batches, resulting in extreme memory spikes.  If I find a workaround to this issue I will
   update this code. ]]